export type Persona = {
  id: string;
  name: string;
  age: number;
  gender: string;
  location: string;
  education: string;
  maritalStatus: string;
  occupation: string;
  incomeLevel: number;
  ethnicGroup: string;
  religion: string;
  description: string; // On harmonise : dans le backend, on utilise "description"
};